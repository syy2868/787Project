// This file is generated by FBC.

#include "diverter.h"
#include <string.h>
#include <stdio.h>

/* Function block initialization function */
void diverterinit(diverter* me)
{
    me->_state = 0;
    me->_entered = false;
    me->_input.events = 0;
    me->_output.events = 0;
}

/* ECC algorithms */
void diverter_init(diverter* me)
{
me->theta=6;
me->y=0;
}

void diverter_toStorage1(diverter* me)
{
if (me->y == 0){
 printf("diverter already in storage1, control error \n");
 //exit(1);
 }

me->y=me->y-2;
printf("diverter, %f\n",me->y);

}

void diverter_toStorage2(diverter* me)
{
if (me->y == me->theta){
 printf("diverter already in storage2, control error \n");
 //exit(1);
 }


me->y=me->y+2;
printf("diverter, %f\n",me->y);
}

/* Function block execution function */
void diverterrun(diverter* me)
{
    me->_output.events = 0;

    for (;;) {
        switch (me->_state) {
            case 0:
                // State: Start
                if (!me->_entered) {
                    diverter_init(me);
                    me->_output.event.update = 1;
                    me->_entered = true;
                }
                else {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
                break;
            case 1:
                // State: State1
                if (!me->_entered) {
                    me->_entered = true;
                }
                else {
                    if (me->_input.event.S2) {
                        me->_state = 2;
                        me->_entered = false;
                        continue;
                    }
                    else if (me->_input.event.S3) {
                        me->_state = 3;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 2:
                // State: State2
                if (!me->_entered) {
                    diverter_toStorage1(me);
                    me->_output.event.update = 1;
                    me->_entered = true;
                }
                else {
                    if (me->y <= 0) {
                        me->_state = 1;
                        me->_entered = false;
                        continue;
                    }
                    else {
                        me->_state = 2;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 3:
                // State: State3
                if (!me->_entered) {
                    diverter_toStorage2(me);
                    me->_output.event.update = 1;
                    me->_entered = true;
                }
                else {
                    if (me->y >= me->theta) {
                        me->_state = 1;
                        me->_entered = false;
                        continue;
                    }
                    else {
                        me->_state = 3;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
        }
        break;
    }
    if (me->_output.event.update) {
        me->_y = me->y;
    }

    me->_input.events = 0;
}

