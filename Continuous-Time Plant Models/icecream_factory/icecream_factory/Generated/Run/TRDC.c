// This file is generated by FBC.

#include "TRDC.h"
#include <string.h>
#include <stdio.h>

/* Function block initialization function */
void TRDCinit(TRDC* me)
{
    me->_state = 0;
    me->_entered = false;
    me->_input.events = 0;
    me->_output.events = 0;
}

/* ECC algorithms */
void TRDC_init(TRDC* me)
{
me->ice_id=2;
}

void TRDC_random_icecream(TRDC* me)
{
/* Intializes random number generator */
srand(time(NULL));

me->ice_id = rand() % 2 ;

//printf("%d, %f\n", me->ice_id, me->y);
}

void TRDC_Algorithm1(TRDC* me)
{
//printf("S2\n");
printf("Send signal: divert to the storage1\n");
}

void TRDC_Algorithm2(TRDC* me)
{
//printf("S3\n");
printf("Send signal: divert to the storage2\n");
}

/* Function block execution function */
void TRDCrun(TRDC* me)
{
    me->_output.events = 0;

    if (me->_input.events) {
        if (me->_input.event.update) {
            me->y = me->_y;
        }
    }
    for (;;) {
        switch (me->_state) {
            case 0:
                // State: Start
                if (!me->_entered) {
                    TRDC_init(me);
                    me->_entered = true;
                }
                else {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
                break;
            case 1:
                // State: State1
                if (!me->_entered) {
                    TRDC_random_icecream(me);
                    me->_entered = true;
                }
                else {
                    if (me->_input.event.S1 && (me->ice_id == 1 && me->y == 6)) {
                        me->_state = 2;
                        me->_entered = false;
                        continue;
                    }
                    else if (me->_input.event.S1 && (me->ice_id == 0 && me->y == 0)) {
                        me->_state = 3;
                        me->_entered = false;
                        continue;
                    }
                    else {
                        me->_state = 1;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 2:
                // State: State2
                if (!me->_entered) {
                    TRDC_Algorithm1(me);
                    me->_output.event.S2 = 1;
                    me->_entered = true;
                }
                else {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
                break;
            case 3:
                // State: State3
                if (!me->_entered) {
                    TRDC_Algorithm2(me);
                    me->_output.event.S3 = 1;
                    me->_entered = true;
                }
                else {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
                break;
        }
        break;
    }

    me->_input.events = 0;
}

