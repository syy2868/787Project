// This file is generated by FBC.

#include "pH_control.h"
#include <string.h>
#include <stdio.h>

/* Function block initialization function */
void pH_controlinit(pH_control* me)
{
    me->_state = 0;
    me->_entered = false;
    me->_input.events = 0;
    me->_output.events = 0;
}

/* ECC algorithms */
void pH_control_init(pH_control* me)
{
me->x2=7.1;
}

void pH_control_signal_base(pH_control* me)
{
printf("Send signal: pour base. Current pH: %f\n", me->x2);
}

void pH_control_signal_neutral(pH_control* me)
{
printf("Send signal: pour neutral. Current pH: %f\n", me->x2);
}

void pH_control_signal_acid(pH_control* me)
{
printf("Send signal: pour acid. Current pH: %f\n", me->x2);
}

/* Function block execution function */
void pH_controlrun(pH_control* me)
{
    me->_output.events = 0;

    if (me->_input.event.update) {
        me->x2 = me->_x2;
    }
    for (;;) {
        switch (me->_state) {
            case 0:
                // State: Start
                if (!me->_entered) {
                    pH_control_init(me);
                    me->_entered = true;
                }
                else {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
                break;
            case 1:
                // State: State1
                if (!me->_entered) {
                    me->_entered = true;
                }
                else {
                    if (true && (me->x2 <= 6.8)) {
                        me->_state = 2;
                        me->_entered = false;
                        continue;
                    }
                    else if (true && (me->x2 >= 7.2)) {
                        me->_state = 4;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 2:
                // State: State2
                if (!me->_entered) {
                    pH_control_signal_base(me);
                    me->_output.event.pbase = 1;
                    me->_entered = true;
                }
                else {
                    if (true && (me->x2 >= 7)) {
                        me->_state = 3;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 3:
                // State: State3
                if (!me->_entered) {
                    pH_control_signal_neutral(me);
                    me->_output.event.pneutral = 1;
                    me->_entered = true;
                }
                else {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
                break;
            case 4:
                // State: State4
                if (!me->_entered) {
                    pH_control_signal_acid(me);
                    me->_output.event.pacid = 1;
                    me->_entered = true;
                }
                else {
                    if (true && (me->x2 <= 7)) {
                        me->_state = 3;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
        }
        break;
    }

    me->_input.events = 0;
}

