// This file is generated by FBC.

#include "controller.h"
#include <string.h>
#include <stdio.h>

/* Function block initialization function */
void controllerinit(controller* me)
{
    me->_state = 0;
    me->_entered = false;
    me->_input.events = 0;
    me->_output.events = 0;
}

/* ECC algorithms */
void controller_init(controller* me)
{
me->x=510;
me->c=20;

}

void controller_signal_add1(controller* me)
{
printf("Send signal: add1, Temperature : %f\n",me->x);
}

void controller_signal_add2(controller* me)
{
printf("Send signal: add2, Temperature : %f\n",me->x);
}

void controller_signal_remove1(controller* me)
{
printf("Send signal: remove1, Temperature : %f\n",me->x);
}

void controller_signal_remove2(controller* me)
{
printf("Send signal: remove2, Temperature : %f\n",me->x);
}

/* Function block execution function */
void controllerrun(controller* me)
{
    me->_output.events = 0;

    if (me->_input.event.clock) {
        me->x = me->_x;
        me->timer1 = me->_timer1;
        me->timer2 = me->_timer2;
    }
    for (;;) {
        switch (me->_state) {
            case 0:
                // State: Start
                if (!me->_entered) {
                    controller_init(me);
                    me->_entered = true;
                }
                else {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
                break;
            case 1:
                // State: State1
                if (!me->_entered) {
                    me->_entered = true;
                }
                else {
                    if (true && (me->x >= 550 && me->timer2 >= me->c)) {
                        me->_state = 2;
                        me->_entered = false;
                        continue;
                    }
                    else if (true && (me->x >= 550 && me->timer1 >= me->c && me->timer2 <= me->c)) {
                        me->_state = 4;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 2:
                // State: State3
                if (!me->_entered) {
                    controller_signal_add2(me);
                    me->_output.event.add2 = 1;
                    me->_entered = true;
                }
                else {
                    if (true && (me->x <= 510)) {
                        me->_state = 3;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 3:
                // State: State5
                if (!me->_entered) {
                    controller_signal_remove2(me);
                    me->_output.event.remove2 = 1;
                    me->_output.event.refresh2 = 1;
                    me->_entered = true;
                }
                else {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
                break;
            case 4:
                // State: State2
                if (!me->_entered) {
                    controller_signal_add1(me);
                    me->_output.event.add1 = 1;
                    me->_entered = true;
                }
                else {
                    if (true && (me->x <= 510)) {
                        me->_state = 5;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 5:
                // State: State4
                if (!me->_entered) {
                    controller_signal_remove1(me);
                    me->_output.event.remove1 = 1;
                    me->_output.event.refresh1 = 1;
                    me->_entered = true;
                }
                else {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
                break;
        }
        break;
    }

    me->_input.events = 0;
}

