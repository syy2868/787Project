// This file is generated by FBC.

#include "GasBurner.h"
#include <string.h>
#include <stdio.h>

/* Function block initialization function */
void GasBurnerinit(GasBurner* me)
{
    me->_state = 0;
    me->_entered = false;
    me->_input.events = 0;
    me->_output.events = 0;
    me->_simdata = mxmlNewElement( MXML_NO_PARENT, "Instance"); // inited to main's tree later.. but make sure it's usable without
}

/* ECC algorithms */
void GasBurner_Init(GasBurner* me)
{
me->y=0;
me->d=0.01;
}

void GasBurner_Timer(GasBurner* me)
{
me->y=(float) me->y+me->d;
//printf("timer : %f\n", me->y);
}

void GasBurner_Reset(GasBurner* me)
{
//printf("signal transfered\n");
me->y=0;
}

/* Function block execution function */
void GasBurnerrun(GasBurner* me)
{
    clearChildren(me->_simevents);
    clearChildren(me->_simvars);
    deleteNexts(me->_simstate);
    clearChildren(me->_simtransitions);
    me->_output.events = 0;

    for (;;) {
        switch (me->_state) {
            case 0:
                // State: Start
                mxmlElementSetAttr(me->_simstate, "Name", "Start");
                if (!me->_entered) {
                    GasBurner_Init(me);
                    me->_entered = true;
                }
                else {
                    if (me->_input.event.TurnOn) {
                        appendTransitionToXML(me->_simtransitions, "Start", "State1", "TurnOn");
                        me->_state = 1;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 1:
                // State: State1
                mxmlElementSetAttr(me->_simstate, "Name", "State1");
                if (!me->_entered) {
                    GasBurner_Timer(me);
                    me->_entered = true;
                }
                else {
                    if (me->y >= 0.1) {
                        appendTransitionToXML(me->_simtransitions, "State1", "State2", "y>=0.1");
                        me->_state = 2;
                        me->_entered = false;
                        continue;
                    }
                    else {
                        appendTransitionToXML(me->_simtransitions, "State1", "State1", "1");
                        me->_state = 1;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 2:
                // State: State2
                mxmlElementSetAttr(me->_simstate, "Name", "State2");
                if (!me->_entered) {
                    GasBurner_Reset(me);
                    me->_output.event.On = 1;
                    me->_entered = true;
                }
                else {
                    if (me->_input.event.TurnOff) {
                        appendTransitionToXML(me->_simtransitions, "State2", "State3", "TurnOff");
                        me->_state = 3;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 3:
                // State: State3
                mxmlElementSetAttr(me->_simstate, "Name", "State3");
                if (!me->_entered) {
                    GasBurner_Timer(me);
                    me->_entered = true;
                }
                else {
                    if (me->y >= 0.1) {
                        appendTransitionToXML(me->_simtransitions, "State3", "State4", "y>=0.1");
                        me->_state = 4;
                        me->_entered = false;
                        continue;
                    }
                    else {
                        appendTransitionToXML(me->_simtransitions, "State3", "State3", "1");
                        me->_state = 3;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 4:
                // State: State4
                mxmlElementSetAttr(me->_simstate, "Name", "State4");
                if (!me->_entered) {
                    GasBurner_Reset(me);
                    me->_output.event.Off = 1;
                    me->_entered = true;
                }
                else {
                    if (me->_input.event.TurnOn) {
                        appendTransitionToXML(me->_simtransitions, "State4", "State1", "TurnOn");
                        me->_state = 1;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
        }
        break;
    }
    if (me->_output.event.Update) {
        me->_y = me->y;
    }

    GasBurner_runXML(me);
    me->_input.events = 0;
}

void GasBurner_initXML(mxml_node_t* node, GasBurner* me, char* instancename)
{
    // Add to tree
    mxmlAdd(node, MXML_ADD_AFTER, NULL, me->_simdata);
    // Add constanty things
    mxmlElementSetAttrf(me->_simdata, "Name", "%s", instancename);
    mxmlElementSetAttr(me->_simdata, "Type", "GasBurner");
    me->_simevents = mxmlNewElement(me->_simdata,"Events");
    me->_simvars = mxmlNewElement(me->_simdata,"Vars");
    me->_simstates = mxmlNewElement(me->_simdata,"ECStates");
    me->_simstate = mxmlNewElement(me->_simstates,"ECState");
    me->_simtransitions = mxmlNewElement(me->_simdata,"ECTransitions");
}

void GasBurner_runXML(GasBurner* me)
{
mxml_node_t* childnode; // used as a temp node
childnode = mxmlNewElement(me->_simevents,"Event");
mxmlElementSetAttr(childnode, "Name", "TurnOn");
mxmlElementSetAttr(childnode, "Scope", "Input");
mxmlElementSetAttr(childnode, "Type", "EVENT");
if(  me->_input.event.TurnOn )
    mxmlElementSetAttr(childnode, "Value", "Present");
else
    mxmlElementSetAttr(childnode, "Value", "Absent");
childnode = mxmlNewElement(me->_simevents,"Event");
mxmlElementSetAttr(childnode, "Name", "TurnOff");
mxmlElementSetAttr(childnode, "Scope", "Input");
mxmlElementSetAttr(childnode, "Type", "EVENT");
if(  me->_input.event.TurnOff )
    mxmlElementSetAttr(childnode, "Value", "Present");
else
    mxmlElementSetAttr(childnode, "Value", "Absent");
childnode = mxmlNewElement(me->_simevents,"Event");
mxmlElementSetAttr(childnode, "Name", "On");
mxmlElementSetAttr(childnode, "Scope", "Output");
mxmlElementSetAttr(childnode, "Type", "EVENT");
if(  me->_output.event.On )
    mxmlElementSetAttr(childnode, "Value", "Present");
else
    mxmlElementSetAttr(childnode, "Value", "Absent");
childnode = mxmlNewElement(me->_simevents,"Event");
mxmlElementSetAttr(childnode, "Name", "Off");
mxmlElementSetAttr(childnode, "Scope", "Output");
mxmlElementSetAttr(childnode, "Type", "EVENT");
if(  me->_output.event.Off )
    mxmlElementSetAttr(childnode, "Value", "Present");
else
    mxmlElementSetAttr(childnode, "Value", "Absent");
childnode = mxmlNewElement(me->_simevents,"Event");
mxmlElementSetAttr(childnode, "Name", "Update");
mxmlElementSetAttr(childnode, "Scope", "Output");
mxmlElementSetAttr(childnode, "Type", "EVENT");
if(  me->_output.event.Update )
    mxmlElementSetAttr(childnode, "Value", "Present");
else
    mxmlElementSetAttr(childnode, "Value", "Absent");
childnode = mxmlNewElement(me->_simvars,"Var");
mxmlElementSetAttr(childnode, "Name", "y");
mxmlElementSetAttr(childnode, "Scope", "Output");
mxmlElementSetAttr(childnode, "Type", "REAL");
mxmlElementSetAttrf(childnode, "Value", "%f", me->_y);
childnode = mxmlNewElement(me->_simvars,"Var");
mxmlElementSetAttr(childnode, "Name", "d");
mxmlElementSetAttr(childnode, "Scope", "Internal");
mxmlElementSetAttr(childnode, "Type", "REAL");
mxmlElementSetAttrf(childnode, "Value", "%f", me->d);
//mxmlElementSetAttrf(me->_simstate, "Name", "%s", me->_statename); // set in run function
}

